const API=(location.protocol==='file:'?'http://localhost:3000':'http://localhost:3000')+'/api';
async function load(){const s=document.querySelector('#status');try{const r=await fetch(API+'/admin/stats');if(!r.ok)throw Error();const d=await r.json();users.textContent=d.users;needs.textContent=d.needs;posts.textContent=d.posts;categories.innerHTML=d.categories.map(x=>`<div class="bar"><b>${x.category}</b><div class="track"><div class="fill" style="width:${Math.min(100,x.total*8)}%"></div></div><span>${x.total}</span></div>`).join('');s.textContent='API conectada correctamente.'}catch{s.textContent='Inicia la API con Docker Compose para ver datos reales.'}}refresh.onclick=load;load();
