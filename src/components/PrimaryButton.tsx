import { Pressable, Text, StyleSheet } from 'react-native'; import { colors } from '../shared/theme';
export function PrimaryButton({label,onPress,disabled}:{label:string;onPress:()=>void;disabled?:boolean}){return <Pressable onPress={onPress} disabled={disabled} style={[s.button,disabled&&s.disabled]}><Text style={s.text}>{label}</Text></Pressable>}; const s=StyleSheet.create({button:{minHeight:50,borderRadius:15,backgroundColor:colors.primary,alignItems:'center',justifyContent:'center',paddingHorizontal:18},text:{color:'#fff',fontSize:15,fontWeight:'800'},disabled:{opacity:.5}});
