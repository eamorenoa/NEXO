import { useState } from 'react'; import { AIService } from '../services/AIService'; import { AIResponse } from '../models/AIResponse';
export function useAIViewModel(token?:string){const [message,setMessage]=useState('');const [response,setResponse]=useState<AIResponse|null>(null);const [loading,setLoading]=useState(false);const [error,setError]=useState('');const ask=async()=>{if(!token||message.trim().length<3)return;setLoading(true);setError('');try{setResponse(await AIService.ask(token,message));}catch(e:any){setError(e.message);}finally{setLoading(false);}};return{message,setMessage,response,loading,error,ask};}
